Type-only identity: name in the handle register, role in the truth register. There is no monogram — do not draw one.
```jsx
<Wordmark />                         // name + role lines
<Wordmark variant="lockup" size={20}/> // name on a threshold, for footers and slide corners
<Wordmark variant="name" size={64}/>
```