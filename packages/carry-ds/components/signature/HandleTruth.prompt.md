The handle/truth form — for correcting a misleading label, proxy or surface belief. For other relationships (claim/support, outcome/enabled, question/conditions…) use `Composition`.
```jsx
<HandleTruth handle="An AI assistant." handleSize="xl"
  truth={<>three support flows · one question in all of them — illustrative example</>}
  decision="build the status lookup first; add conversation where it earns its place." />
```
Example content in this system is illustrative unless labelled as verified Salman evidence.