{
  "name": "carry-elements",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Portable layer: native custom elements that render Carry's grammar without React. Requires packages/carry-ds/styles.css on the page.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "scripts": { "build": "tsc -p tsconfig.build.json", "typecheck": "tsc -p tsconfig.json" },
  "dependencies": { "carry-content": "workspace:*" }
}
