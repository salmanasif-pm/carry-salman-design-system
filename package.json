{
  "name": "carry",
  "version": "0.9.1",
  "private": true,
  "description": "Code home for Carry, Salman Asif's personal design system. packages/carry-ds is canonical; everything else is tooling around it.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check:release": "node scripts/release-gate.mjs",
    "check:deps": "node scripts/check-deps.mjs",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "build": "pnpm -r --if-present build",
    "dev": "pnpm --filter carry-web dev",
    "render": "node packages/carry-render/bin/carry-render.mjs",
    "check:mobile": "node packages/carry-render/bin/carry-check-mobile.mjs",
    "render:examples": "for f in packages/carry-content/examples/*.json; do node packages/carry-render/bin/carry-render.mjs \"$f\" --all || exit 1; done",
    "claims:sync": "node packages/carry-content/bin/claims-sync.mjs",
    "check": "pnpm check:release && pnpm check:deps && pnpm build && pnpm typecheck && pnpm test",
    "validate:briefs": "for f in packages/carry-content/fixtures/*.json; do node packages/carry-content/bin/validate-brief.js \"$f\" || exit 1; done"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
